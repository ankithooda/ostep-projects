/*
    Refresher of Chapter 5 K&R C.
*/
#include <stdio.h>

char* my_strtok(char* , const char*;

int main() {
    int x = 5;
    int *p = &x;
    int **pp = &p;
    char *buffer = NULL;

    printf("%d-%d-%d\n", x, *p, **pp);
    printf("%s\n", take_input(buffer));
    return 0;
}

char* my_strtok(char* s, const char* delim) {
    char c = 'X';
    return &c;
}